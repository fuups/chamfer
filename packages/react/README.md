# @chamfer/react

React bindings for Chamfer UI. Components wrap the framework-agnostic CSS (`@chamfer/core`) and progressive enhancement helpers (`@chamfer/behavior`) while exposing idiomatic React props.

> Status: placeholder implementation. APIs will expand once the underlying tokens and behaviors are finalized.
