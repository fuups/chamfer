{
  "name": "@chamfer/behavior",
  "version": "0.0.0",
  "description": "Progressive enhancement helpers for Chamfer UI components.",
  "type": "module",
  "files": [
    "dist",
    "src"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "keywords": [
    "progressive-enhancement",
    "design-system",
    "chamfer"
  ],
  "devDependencies": {
    "jsdom": "^24.1.1",
    "vitest": "^2.1.4"
  },
  "license": "MIT"
}
