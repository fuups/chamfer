# @chamfer/angular

Angular directives and modules for Chamfer UI. The package wraps the shared CSS and behavior helpers, providing Angular-friendly APIs while keeping the underlying implementation aligned across frameworks.

> Status: scaffolding only. Real components will be implemented once the tokens and core styles are in place.
